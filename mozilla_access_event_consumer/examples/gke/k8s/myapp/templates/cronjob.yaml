---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "application.fullname" . }}-employee-exits
  labels:
    {{- include "application.labels" . | nindent 4 }}
spec:
  schedule: "0 0 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          name: {{ include "application.fullname" . }}-employee-exits
        spec:
          serviceAccountName: {{ include "application.fullname" . }}
          containers:
            - name: employee-exits
              imagePullPolicy: Always
              image: "{{ .Values.employeeExitsImage.repository }}:{{ .Values.employeeExitsImage.tag }}"
              env:
                - name: PROJECT_ID
                  value: {{ .Values.projectId }}
                - name: SUBSCRIPTION_ID
                  value: {{ .Values.employeeExitsSubscriptionId }}
                - name: LOG_LEVEL
                  value: "INFO"
              {{- if .Values.employeeExits.secrets.enabled }}
              envFrom:
                - secretRef:
                    name: {{ include "application.fullname" . }}-employee-exits-secrets
              {{- end }}
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "250m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
              securityContext:
                runAsUser: 1000
                runAsGroup: 1000
                allowPrivilegeEscalation: false
                seccompProfile:
                  type: "RuntimeDefault"
                runAsNonRoot: true
                capabilities:
                  drop: ["ALL"]
          restartPolicy: OnFailure
